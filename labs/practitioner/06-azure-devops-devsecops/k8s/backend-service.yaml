apiVersion: v1
kind: Service
metadata:
  name: node-backend
  namespace: practitioner-azure-devops-devsecops
spec:
  selector:
    app: node-backend
  ports:
    - name: http
      port: 80
      targetPort: 5000
