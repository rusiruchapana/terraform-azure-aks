apiVersion: v1
kind: Service
metadata:
  name: gitlab-devsecops-demo
  namespace: practitioner-gitlab-devsecops
spec:
  type: ClusterIP
  selector:
    app: gitlab-devsecops-demo
  ports:
    - port: 80
      targetPort: 80
