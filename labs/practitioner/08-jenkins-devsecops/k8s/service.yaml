apiVersion: v1
kind: Service
metadata:
  name: jenkins-devsecops-demo
  namespace: practitioner-jenkins-devsecops
spec:
  type: ClusterIP
  selector:
    app: jenkins-devsecops-demo
  ports:
    - port: 80
      targetPort: 80
