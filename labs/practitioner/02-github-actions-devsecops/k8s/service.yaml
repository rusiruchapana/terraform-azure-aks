apiVersion: v1
kind: Service
metadata:
  name: github-actions-devsecops-demo
  namespace: practitioner-github-actions-devsecops
spec:
  type: ClusterIP
  selector:
    app: github-actions-devsecops-demo
  ports:
    - port: 80
      targetPort: 80
