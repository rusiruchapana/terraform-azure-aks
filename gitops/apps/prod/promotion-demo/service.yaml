apiVersion: v1
kind: Service
metadata:
  name: promotion-demo
  namespace: promotion-prod
  labels:
    app: promotion-demo
    environment: prod
spec:
  type: ClusterIP
  selector:
    app: promotion-demo
    environment: prod
  ports:
    - name: http
      port: 80
      targetPort: 80
